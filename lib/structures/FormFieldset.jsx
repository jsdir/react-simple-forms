/*
import React from 'react';

import updateCursors from '../utils/updateCursors';

export default React.createClass({

  mixins: [FormMixin],

  propTypes: {
    name: React.PropTypes.string.isRequired
  },

  render() {
    return <div>
      this.updateCursors(this.props.children, [this.props.name]);
    </div>;
  }
});
*/
